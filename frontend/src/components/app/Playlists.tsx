import {Card, CardContent, CardTitle} from "@/components/ui/card.tsx";


function Playlists() {
    return <Card className="flex-1 overflow-y-auto h-full">
        <CardTitle className="text-4xl m-4">Playlists</CardTitle>
        <CardContent>
            <p>Not Implemented</p>
        </CardContent>
    </Card>;
}

export default Playlists;
